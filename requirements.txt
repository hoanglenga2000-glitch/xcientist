# Control-plane runtime dependencies for the research workstation.
# These are what the local orchestrator, verification scripts, report builders,
# and GPU-gateway/SSH layer need. Heavy model-training libraries that run on the
# remote GPU server live in requirements-gpu.txt; test tooling in requirements-dev.txt.
#
# Pins use compatible-release (~=) ranges validated against the local baseline
# (Python 3.11) and the remote GPU server (Python 3.10.12).

pandas>=2.0,<4.0
numpy>=1.24,<3.0
scikit-learn>=1.3,<2.0
PyYAML>=6.0,<7.0
python-docx>=1.1,<2.0
kaggle>=1.6,<3.0
paramiko>=3.0,<6.0        # GPU SSH gateway (used by ~26 control-plane scripts)
