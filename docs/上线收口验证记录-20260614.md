# 科研 Agent 工作站上线收口验证记录 - 2026-06-14

## 结论

当前版本达到“本地可安全演示、可完整运行、外部资源边界清晰”的上线状态。

不能标记为完全外部资源上线的部分仍然保持可见阻塞：Claude Code 缺少 Anthropic Key，Kaggle 官方链路缺少 token，GPU 自动 SSH 作业仍等待远端公钥授权。DeepSeek 已通过 Windows DPAPI 注入并完成真实 smoke。

## 本轮新增证据

- Verified launcher 已重新启动服务：`scripts/start_verified_workstation.ps1 restart -Port 8088 -SkipFullAcceptance`
- Dashboard：`http://127.0.0.1:8088`
- `/api/workstation-summary` 已返回 `verified_launch_audit.status=passed`
- 启动审计：`docs/verified_workstation_launch_audit.json`
- 启动审计摘要：`docs/verified_workstation_launch_audit.md`
- 全量验收：`python scripts/run_full_acceptance.py --dashboard-url http://127.0.0.1:8088`，结果 `passed`，`checks_run=38`
- UI polish 验证：`python scripts/verify_scientific_ui_polish.py`，结果 `passed`
- 启动审计验证：`python scripts/verify_verified_workstation_launch_audit.py`，结果 `passed`
- 明文密钥扫描：`python scripts/verify_no_plaintext_secrets.py`，结果 `passed`
- 精确口令标记扫描：扫描 45424 个文件，3 个敏感标记命中 0

## 视觉验收

- Chrome 桌面截图：`docs/visual_acceptance_desktop.png`
- Chrome 移动截图：`docs/visual_acceptance_mobile.png`
- Figma 当前页面截图留证：`docs/figma_current_page_capture.png`
- Figma 设计板：`https://www.figma.com/design/3SctfaIfPKAbovNBPnKxjq`
- Figma 页面节点：`https://www.figma.com/design/3SctfaIfPKAbovNBPnKxjq?node-id=2-2`

桌面截图确认首屏展示：

- DeepSeek：就绪
- GPU/HPC：已验证 4 x A800，但自动 SSH 作业仍等待远端公钥授权
- Claude Code：未配置
- Kaggle：未配置
- 启动审计卡片：通过，显示 DS on、Claude off、Kaggle off、5 项 smoke 已记录

移动截图确认顶部导航、搜索、状态条和 Mission Control 首屏没有明显遮挡或文字重叠。

## 浏览器工具说明

Browser 插件本轮可以打开 `http://127.0.0.1:8088`，但截图接口返回 `Root CDP client not initialized`。因此视觉证据改由本机 Chrome headless 生成，并由 DOM 关键文案断言确认页面状态。该问题属于浏览器插件会话层异常，不是工作站页面服务异常。

## 真实资源边界

- DeepSeek：已接入，DPAPI loaded = true，smoke passed。
- GPU：硬件与 Web Terminal 证据已验证，容量为 4 x NVIDIA A800；自动 SSH job 未完全打通，等待远端公钥授权。
- Claude Code：未接入，等待 Anthropic API Key，不能标记 ready。
- Kaggle 官方：未接入，等待 `KAGGLE_USERNAME` / `KAGGLE_KEY`，官方下载和提交必须保留 Human Gate。
- 本地 Kaggle-style 训练：可运行；House Prices 最新本地分数为 `cv_rmsle_mean=0.12410`，run `20260613_232324`。

## 下一步上线操作

正式演示或交付前优先使用：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File scripts\start_verified_workstation.ps1 restart -Port 8088
python scripts\run_full_acceptance.py --dashboard-url http://127.0.0.1:8088
```

外部资源补齐后再分别运行对应 `smoke -AllowRealExternal`，未通过前继续保持 UI 的 Not Configured / Pending 状态。
