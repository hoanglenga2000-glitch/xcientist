# 可视化验收记录 - 2026-06-12 科研质感补充

## 验收信息

- 验收地址：`http://127.0.0.1:8088`
- 验收方式：本机 Chrome headless 截图 + API/静态验收脚本
- 目标：补强科研工作站质感，并确认移动端不会出现文字竖排、内容遮挡或关键入口不可读。

## 截图证据

- 桌面端科研质感截图：`docs/scientific_ui_polish_20260612.png`
- 移动端修复前截图：`docs/scientific_ui_mobile_20260612.png`
- 移动端修复后截图：`docs/scientific_ui_mobile_fixed_20260612.png`
- Figma 当前页面捕获：`https://www.figma.com/design/3SctfaIfPKAbovNBPnKxjq?node-id=2-2`

## 本轮视觉修复

- 新增科研运行状态条：`CV Gate`、`证据索引`、`人工 Gate`、`上线边界`。
- 新增 Mission Control 首屏 `AI Workstation Command Deck`，把当前任务、最新 run、主指标、4 x A800、任务/运行/动作统计和资源激活数集中到一个工作站指挥区。
- 新增 `launch-evidence-strip`：DeepSeek、HPC/GPU、Claude Code、Kaggle 均显示下一证据、真实状态和不含密钥的操作提示，避免未配置资源被误读为 ready。
- 新增 `Verified Launch Audit` 卡片：首屏直接显示最近一次 verified launcher 审计状态、DPAPI 加载布尔值、smoke 检查数量和审计报告路径。
- 使用 Figma 插件捕获当前本地页面作为设计评审参考；capture script 只用于临时捕获，完成后已从生产代码移除。
- 移动端 ResearchBrief 从多列硬挤改为横向科研摘要轨道，避免研究问题、研究目标等文字被压成竖排。
- 移动端侧栏导航改为默认折叠的顶部菜单，避免手机首屏先被完整导航区占满。
- 桌面端仍保持多列科研摘要仪表板，不降低信息密度。

## 验收结论

- 桌面端：首屏可以同时查看工作站指挥区、任务队列、训练结果、接入状态、下一证据行动条、动作日志和科研状态条。
- 移动端：默认显示紧凑导航条、搜索、科研状态条和研究摘要卡片；ResearchBrief 不再竖排压缩，完整导航可通过菜单展开。
- 安全边界：GPU/Kaggle 仍保持受控未配置，本地训练、报告、证据链和 Gate 继续可运行。

## 对应自动化检查

```powershell
python scripts\verify_scientific_ui_polish.py
python scripts\verify_ui_layout_quality.py
python scripts\run_full_acceptance.py --dashboard-url http://127.0.0.1:8088
```
## 2026-06-14 视觉复核补充

- 本机 Chrome headless 已重新生成桌面截图：`docs/visual_acceptance_desktop.png`。
- 本机 Chrome headless 已重新生成移动截图：`docs/visual_acceptance_mobile.png`。
- Figma 插件已读取当前页面节点并保存截图：`docs/figma_current_page_capture.png`。
- 桌面截图确认“上线资源总控”中可见启动审计卡片，显示 `DS on`、`Claude off`、`Kaggle off` 和 5 项 smoke 记录。
- DOM 文案断言通过：DeepSeek ready、GPU verified、Claude not configured、Kaggle not configured、启动审计卡片可见。
- Browser 插件截图接口本轮异常，已使用 Chrome headless 作为替代可视化验证路径。
