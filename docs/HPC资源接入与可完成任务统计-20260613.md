# HPC 资源接入与可完成任务统计 - 2026-06-13

## 结论

当前已经确认 HKUST(GZ) HPC+AI 智算中心的 Web 入口、代理链路和 SSH 网关网络均可达。Web 管理桌面已经通过账号手动登录成功；SSH 网关能完成握手，并确认密码和本机公钥均被服务端识别为 `partial success`，但还没有拿到完整 shell，因此暂时不能通过命令行枚举 GPU 节点、Slurm 队列和服务器目录内容。

这意味着：现在可以继续通过 Web 资源中心查看和申请资源；科研工作站的 GPU SSH 真接入还需要解决 SSH 组合认证/公钥绑定问题。

## 按指南核对的连接信息

- SOCKS5 代理服务器：指南要求通过代理访问 HPC 资源。
- HPC Web 管理界面：`http://100.85.169.63:1234/appform/login`
- HPC SSH 登录节点：`100.85.169.63:1235`
- 本机 HPC 专用 Chrome 快捷方式：`D:\桌面\HPC管理界面.lnk`
- 本机快捷方式参数：`--proxy-server=socks5://127.0.0.1:7897 --user-data-dir="C:\Users\景浩伟\hpc-chrome-data" http://100.85.169.63:1234/appform/login`

## 已验证通过

- 代理端口监听：
  - `127.0.0.1:7892` 正在监听。
  - `127.0.0.1:7897` 正在监听。
- 网络连通：
  - 代理服务器端口可达。
  - HPC Web `100.85.169.63:1234` 可达。
  - HPC SSH `100.85.169.63:1235` 可达。
- SSH 网关：
  - 通过本地 Clash `7897` 可读到 SSH banner：`SSH-2.0-SSHPiper`。
  - 通过指南 SOCKS5 代理也可读到 SSH banner：`SSH-2.0-SSHPiper`。
- Web 访问：
  - Chrome History 记录显示已访问 `http://100.85.169.63:1234/appform/desktop`。
  - 截图显示已经进入 `HKUST(GZ) HPC+AI智算中心平台` 桌面。

## SSH 当前阻塞点

OpenSSH 通过 SOCKS ProxyCommand 后能完成握手和 host key 校验，但认证最终未完成。关键日志语义如下：

- `Authenticated using "password" with partial success.`
- `Server accepts key ... Authenticated using "publickey" with partial success.`
- 最终仍返回：`Permission denied (password,publickey).`

这说明不是网络不通，也不是单纯密码错误。服务端看起来要求组合认证，但当前账号/公钥/认证顺序没有完成最终授权。建议向管理员确认：

- `aimslab` 是否已开通 SSH shell 权限。
- 当前 Web 密码是否同步到 SSH 网关。
- 是否必须先在 Web 平台绑定 SSH 公钥。
- 需要绑定哪一个本机公钥，建议优先使用 `C:\Users\景浩伟\.ssh\id_ed25519.pub`。
- 是否存在二次认证、审批或首次登录激活步骤。

## 目前 Web 桌面可见功能入口

从当前资源中心截图可确认以下功能入口已经可见：

- `我的作业`：查看、提交或管理计算作业。
- `我的镜像`：查看或管理容器/运行镜像。
- `我的会话`：进入交互式会话。
- `用户费用`：查看资源费用或用量计费。
- `集群状态`：查看集群/节点状态。
- `Terminal-48h空闲自动回收`：Web 终端入口，空闲 48 小时自动回收。
- `我的数据`：数据目录/文件管理入口。
- `期登录shell`：Shell 登录入口。
- `应用仓库`：可用应用或模板入口。
- `问题工单`：提交平台问题。
- `模型开发`：模型开发环境入口。
- `退费申请记录`：费用相关申请记录。

## 现在可以推进的任务

1. Web 侧资源中心盘点：
   - 打开 `我的数据`，统计可见目录、数据集、存储位置、容量和权限。
   - 打开 `集群状态`，统计可用队列、CPU/GPU 节点、空闲/占用状态。
   - 打开 `我的镜像`，统计可用镜像、CUDA/Python/深度学习框架版本。
   - 打开 `我的作业`，查看是否已有作业模板或历史作业。

2. 科研工作站接入准备：
   - 将 GPU 网关目标确定为 `100.85.169.63:1235`。
   - 将连接方式设为 SOCKS5 ProxyCommand。
   - 在 SSH 完整登录前，系统应继续显示 GPU `Not Configured / SSH auth incomplete`。
   - SSH 完整登录后，再运行 `sinfo`、`squeue`、`nvidia-smi`、`df -hT`、`module avail` 等命令。

3. 数据训练流程：
   - 当前本地科研工作站已经能跑 Titanic、House Prices、Telco Churn 和 Kaggle-style smoke 任务。
   - HPC SSH 打通后，可以把更重的模型搜索、seed sweep、深度学习训练提交到 GPU 队列。
   - Kaggle token 配置后，可以从本地/服务器完成官方下载、训练、submission 生成和人工 Gate 后提交。

## 暂时不能完成的任务

- 不能通过 SSH 命令行枚举真实 GPU 节点，因为 SSH shell 尚未完整授权。
- 不能确认 `我的数据` 下具体数据集名称和目录，因为浏览器插件当前 `Transport closed`，无法代点已登录 Web 页面。
- 不能把科研工作站标记为 GPU Fully Ready；当前应标记为网络可达、认证待完成。

## 下一步最短路径

请在已登录的 Web 桌面中依次打开并截图：

1. `我的数据`
2. `集群状态`
3. `我的镜像`
4. `我的作业`

拿到这四张图后，就可以完成完整资源清单：数据目录、存储容量、可用镜像、作业队列、GPU/CPU 资源和可直接接入科研工作站的能力边界。
