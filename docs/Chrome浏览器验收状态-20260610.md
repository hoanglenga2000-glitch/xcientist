# Chrome 浏览器验收状态

- 日期：2026-06-10
- 本地系统地址：http://127.0.0.1:8088
- 目标：按用户要求使用 `@chrome` 对科研 Agent 工作站进行可视化/交互验收。

## 检查结论

Chrome 环境本身可用，但 Codex Chrome Extension 的自动化通信通道当前不可用。

已确认：

- Google Chrome 正在运行。
- Google Chrome 已安装。
- Codex Chrome Extension 已安装并启用，当前选中配置为 `Profile 1`。
- Native host manifest 存在，扩展 ID、host name、允许来源和注册表路径均匹配。
- 已打开 `Profile 1` 的新 Chrome 窗口进行恢复尝试。

当前阻塞：

- Chrome 自动化连接返回：`Browser is not available: extension`。
- 按 Chrome 技能规则，不能自行重装、修复 native host 或改动浏览器配置。
- 当前 Codex 会话未暴露 Chrome extension 控制入口，因此仍无法完成 Chrome 直连点击/截图链路。

## 已完成的兜底验收

在 Chrome 通信通道不可用的情况下，已使用本地服务与合同校验完成等效验收：

- `http://127.0.0.1:8088` 可访问。
- `/api/workstation-summary` 可访问。
- 中文 UI 关键入口已被合同保护：
  - `任务总控台`
  - `任务队列`
  - `运行任务`
  - `系统动作日志`
  - `待配置`
- 接入状态由 API 验证：
  - Claude Code 存在。
  - GPU 状态存在。
  - 未配置外部资源时显示 `Not Configured` / `待配置`，不伪造成功。
- 页面级视觉抽查已完成：
  - 首页任务总控台正常。
  - 代码运行页 IDE 区、终端区、Claude Code 桥、动作日志正常。
  - 设置页语言切换、SQLite 状态、配置分区、动作日志正常。
  - 报告编辑页大纲、在线编辑器、预览、AI 起草、保存、导出、提交、图表插入均显示为中文入口。

## 自动验收命令

```powershell
python scripts\verify_ui_localization_contract.py --url http://127.0.0.1:8088
python scripts\run_full_acceptance.py --dashboard-url http://127.0.0.1:8088 --container-name research-agent-workstation
```

当前 `run_full_acceptance.py` 已将 UI 本地化合同、UI 布局质量合同、Dashboard 生命周期管理、外部资源网关、上线接入硬化、训练验证和动作合同纳入完整验收。

## 下一步

如果需要继续 Chrome 直连截图/点击验收，请在 Codex 插件侧重新连接 Chrome 控制入口后继续；当前系统侧已通过本地页面/API/合同验收兜底。

## 2026-06-11 更新

- 本地工作站地址仍可访问：`http://127.0.0.1:8088`
- `verify_ui_localization_contract.py`、`run_full_acceptance.py`、`run_real_resource_smoke.py` 均已重新执行并通过。
- 浏览器侧已完成对首页、报告页、设置页、完整性 Gate 的视觉抽查，中文界面、按钮动作和未配置资源状态均正常。
- 当前仍未恢复的仅是 Codex 侧 `Chrome` 扩展控制面；因此本地已用浏览器验收兜底，不把未接通的 Chrome 控制伪装为已修复。

## 2026-06-11 追加收口

- 报告编辑、设置、工作流、代码运行和 Agent Runtime 已补充布局质量验收，防止右侧长日志重新压迫主工作区。
- 本地 Dashboard 已补充 `manage_workstation_dashboard.py` 生命周期管理脚本，并纳入 full acceptance。
- 当前 full acceptance 为 28 项检查全部通过，并已包含 Docker service/container 命名一致性检查和本地启动命令合同检查。
