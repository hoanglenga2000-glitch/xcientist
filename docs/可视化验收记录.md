# 科研 Agent 工作站可视化验收记录

## 验收信息

- 验收日期：2026-06-07
- 验收地址：http://127.0.0.1:8088
- 验收方式：Codex in-app browser MCP 打开检查；Microsoft Edge headless 固化桌面与移动端截图。
- 说明：本机 `browser-use` CLI 未安装，因此本次按 browser-use skill 的浏览器验收目标，使用可用的 in-app browser MCP 与本机 Edge headless 完成等效检查。
- 本次重构依据：`D:\下载\科研Agent工作站任务规划书.docx` 已复制到 `docs/科研Agent工作站任务规划书.docx`，页面按该任务规划书和 AI 工作站模板资料重新组织。

## 截图证据

- 桌面视口截图：`docs/visual_acceptance_desktop.png`
- 移动窄屏截图：`docs/visual_acceptance_mobile.png`

截图生成命令：

```powershell
$edge = 'C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe'
& $edge --headless=new --disable-gpu --hide-scrollbars --window-size=1440,1100 --screenshot="docs\visual_acceptance_desktop.png" http://127.0.0.1:8088
& $edge --headless=new --disable-gpu --hide-scrollbars --window-size=390,1000 --screenshot="docs\visual_acceptance_mobile.png" http://127.0.0.1:8088
```

## 浏览器检查摘要

- 首屏非空：通过。页面显示 `科研 Agent 工作站`、任务 Gate `2/2`、阶段审计 `14/14`、科研流程 `7/9`、证据文件数量和服务器私有区 `未改动`。
- 工作站观感：通过。页面已从长报表重构为左侧导航 + 工作站总览 + 任务规划书主线 + 产品模块适配 + 科研流程轨道，更贴近 AI 工作站/Agent 平台展示。
- 计划书主线可见：通过。页面显示 `计划书修订主线`，强调表格 Kaggle 只是第一阶段入口、后续扩展图像/文本/图结构/多模态和论文报告。
- AI 工作站模板适配可见：通过。页面显示 `Agent 工作台`、`任务中心`、`工作流`、`知识库/RAG`、`人工确认与安全门`、`上线验收`。
- 科研流程轨道可见：通过。页面显示任务理解、资料/文献调研、方案设计、数据分析与处理、代码生成与运行、模型训练与验证、结果检查、报告与复盘、人工审核边界。
- 双任务 gate 可见：通过。页面显示 `titanic` 与 `house_prices`，两项均为 `passed`。
- 模板映射可见：通过。页面显示 `多 Agent 调度主管`、`高级数据分析 Agent`、`代码开发 Agent`、`知识库 RAG Agent`、`流程自动化 Agent`。
- 计划完成度可见：通过。任务理解、EDA、特征工程、建模验证、submission 检查、实验记录、scaffold/post-scaffold、第二任务迁移均显示 `passed`。
- 研究依据可见：通过。页面显示 AutoKaggle、Agent K、AutoResearch AI、AutoSOTA、NanoResearch、Kaggle CLI，并保留到一手来源的链接；同时新增 Dify、Flowise、AutoGen Studio、NanoResearch 作为开源界面/流程参考。
- 完整性检查可见：通过。provenance、reproducibility、validity、human oversight、limitations 均显示 `passed`。
- 长期路线图可见：通过。Kaggle token 接入、第三个真实科研数据案例、模型扩展、GPU 搜索、经验库、失败案例库均显示为未来或受控待启用状态。
- 证据链接可打开：通过。浏览器识别到任务规划书 DOCX、实验 gate、task scaffold、workflow audit、experiment log 等本地证据链接。
- 桌面布局：通过。`1440x1100` 截图中首屏主要验收区块完整可读，无明显遮挡。
- 移动布局：通过。`390x1000` 窄屏截图中导航、标题、状态卡片和关键按钮可见，正文换行正常，无明显文本遮挡。

## 边界确认

- 未读取或修改服务器私有 Agent/工作流。
- 未连接 GPU 服务器。
- 未伪造 Kaggle leaderboard；Kaggle token 和官方提交仍是受控未完成项。
- Docker dashboard 仍作为只读展示层，验收脚本在只读模式下运行。
