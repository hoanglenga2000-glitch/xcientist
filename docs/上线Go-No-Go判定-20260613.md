# 上线 Go/No-Go 判定 - 2026-06-13

- 判定时间：2026-06-14T00:01:36
- 总体判定：`go_local_ready_external_pending`
- Dashboard：http://127.0.0.1:8088

## 当前可上线范围

- 本地科研工作站、Kaggle 风格本地训练闭环、报告/Gate/Action Log：可演示、可运行、可验收。
- HKUST(GZ) HPC 网络层：`127.0.0.1:7890` 到 `100.85.169.63:1235` 可达。
- HKUST(GZ) GPU 容器：已通过登录节点 + 环境 SSH 用户进入，`nvidia-smi` 证明 `4 x NVIDIA A800-SXM4-80GB`。
- 外部增强资源：未提供运行时密钥/自动作业凭据的项目必须继续显示待配置，不能伪造 ready。
- 本机正式启动：使用 `scripts\start_verified_workstation.ps1`，启动前加载已安装 DPAPI 凭据，启动后执行 smoke，避免普通重启漏注入 DeepSeek / Claude / Kaggle 密钥。

## 关键状态

- HPC SSH banner：`SSH-2.0-SSHPiper`
- GPU live state：`GPU Verified: 4 x NVIDIA A800-SXM4-80GB via Login Node / Web Terminal`
- DeepSeek live state：`DeepSeek Ready (deepseek-v4-flash)`
- Claude Code live state：`Not Configured`
- Kaggle live state：`Not Configured`
- Verified launcher：`passed`，当前 `dpapi_loaded.deepseek=true`、`dpapi_loaded.claude=false`、`dpapi_loaded.kaggle=false`
- Web Terminal proof：`passed`
- House Prices latest local score：`cv_rmsle_mean=0.12410`，run `20260613_232324`

## 待办但不伪造成 Ready

- GPU hardware/container access is verified, but the remote environment still requires an already-authorized public key before automated SSH jobs can run.

## No-Go 条件

- Claude Code API key is not configured.
- Kaggle official API token is not configured.

## 下一步

1. 在平台 Web UI 或让管理员把本机生成的 `research_agent_hpc_ed25519.pub` 绑定到该 GPU 环境账号。
2. 再将 `GPU_SSH_HOST`、`GPU_SSH_USER`、`GPU_SSH_KEY_PATH`、`GPU_REMOTE_WORKSPACE` 放入环境变量、`*_FILE` 或 `WORKSTATION_SECRET_DIR`。
3. 重新运行 `python scripts\verify_external_resource_gateways.py --url http://127.0.0.1:8088 --allow-real-external`。
4. 保持 GPU Job 入口只允许白名单训练模板，不开放任意 shell。
5. 如需启用 Claude Code，先运行 `powershell -ExecutionPolicy Bypass -File scripts\manage_claude_secret.ps1 install-key -ApiKey <key>`，再运行 `restart-dashboard` 和 `smoke -AllowRealExternal`；未通过前继续保持 `Not Configured`。
6. 如需启用 Kaggle 官方链路，先运行 `powershell -ExecutionPolicy Bypass -File scripts\manage_kaggle_secret.ps1 install-token -Username <username> -Key <key>`，再运行 `smoke -AllowRealExternal`；官方提交仍必须经过人工 Gate。
7. 正式演示或上线前优先运行 `powershell -ExecutionPolicy Bypass -File scripts\start_verified_workstation.ps1 restart -Port 8088`，而不是直接调用普通 dashboard manager。
