# Research Agent Workstation 上线验收记录

日期：2026-06-09

## 当前可用入口

- 本地前端工作站：http://127.0.0.1:3090
- 工作站 API：http://127.0.0.1:3090/api/workstation-summary
- Docker 工作站入口：http://127.0.0.1:8088

## 本轮完成内容

- Titanic 和 House Prices 均已接入 Research Agent Runtime。
- 前端 Mission Control、Research Tasks、Workflow Graph、Code Runner、Agent Runtime、Experiments、Report Studio、Integrity Gates 均优先读取真实 runtime 数据。
- `/api/workstation-summary` 返回 `runtime_by_task.house_prices` 和 `runtime_by_task.titanic`。
- 新增动作契约验收：41 个前端动作均可写入后端 action log 和 artifact。
- 新增 Code Agent patch 生命周期验收：import、review、apply record、rollback、run compare 均可执行。
- Dockerfile 和 docker-compose 已切换为完整 Next 工作站，而不是旧 Python dashboard。
- Docker 镜像已成功构建，容器 `research-agent-workstation` 已启动并通过 healthcheck。
- Kaggle、GPU、外部 LLM 仍保持受控未配置状态，没有伪装成已完成。

## 最新 runtime 结果

- `experiments/house_prices/20260609_225829`
  - 状态：`FINAL_WAITING_APPROVAL`
  - agent trace：7
  - event log：14
  - gate：3
  - CV RMSLE：0.12899

- `experiments/titanic/20260609_231238`
  - 状态：`FINAL_WAITING_APPROVAL`
  - agent trace：7
  - event log：14
  - gate：3
  - CV accuracy：0.824889

## 已通过验收命令

```powershell
npm run typecheck
npm run build
python -m compileall src scripts
python scripts\verify_runtime_completeness.py --tasks titanic house_prices
python scripts\verify_code_agent_patch_lifecycle.py --task-id house_prices
python scripts\verify_dashboard.py --url http://127.0.0.1:3090
python scripts\verify_workstation_action_contract.py --url http://127.0.0.1:3090
python scripts\run_full_acceptance.py --dashboard-url http://127.0.0.1:3090
docker compose -p research-agent-workstation config
docker compose -p research-agent-workstation build
docker compose -p research-agent-workstation up -d
python scripts\run_full_acceptance.py --dashboard-url http://127.0.0.1:8088 --container-name research-agent-workstation
```

## Docker 状态

Docker CLI 已安装，`docker compose config` 已通过。

本轮首次检查时 Docker daemon 未启动，随后已成功唤醒 Docker Desktop，并完成：

```powershell
docker compose -p research-agent-workstation build
docker compose -p research-agent-workstation up -d
```

当前容器状态：

```text
research-agent-workstation   Up (healthy)   0.0.0.0:3090->3090/tcp, 0.0.0.0:8088->3090/tcp
```

Docker 版验收已通过：

```powershell
python scripts\verify_dashboard.py --url http://127.0.0.1:8088
python scripts\verify_workstation_action_contract.py --url http://127.0.0.1:8088 --container-name research-agent-workstation
python scripts\run_full_acceptance.py --dashboard-url http://127.0.0.1:8088 --container-name research-agent-workstation
```

## 受控未完成项

- 官方 Kaggle 下载/leaderboard 提交：未配置 Kaggle token，保持禁用。
- GPU/远程训练：未接入 GPU 服务，保持 mock/reserved。
- 外部 Claude Code/Codex patch 自动应用：已提供 export/import/review/apply-record/rollback/compare 生命周期和动作审计。MVP 为安全起见不自动改写源文件，真实源码变更仍需 Human Gate 后由受控代码代理执行。
