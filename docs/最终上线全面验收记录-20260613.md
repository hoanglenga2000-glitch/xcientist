# 科研 Agent 工作站最终上线全面验收记录 - 2026-06-13

## 验收结论

当前系统达到“本地可完整演示和运行、外部资源配置后可进入真实接入 smoke test”的上线准备状态。系统没有把未配置的 Claude Code、GPU SSH 或 Kaggle 官方提交伪装成已成功接入。

## 今日修正

- 修复 Kaggle 新比赛接入报告的证据链：`docs/kaggle_new_competition_readiness.json` 现在包含 `workspace_json`、`docs_json`、`docs_markdown` 和 `task_json`。
- 对齐最终交付文档的外部资源状态：Claude Code、GPU SSH、Kaggle 官方链路在当前无凭证环境下均显示 `Not Configured`。
- 补强 `scripts/verify_final_delivery_status.py`：后端显示未配置时，最终 Markdown 不能写成 ready。
- 更新最终交付文档：任务汇总数、House Prices 最新实验、新 Kaggle 比赛接入能力和 38 项 acceptance 结果均已同步。
- 补齐 PDF 官方 `ncat + 127.0.0.1:7890` 路径：`ncat` 已安装到用户目录，`ssh hpc-hkust-gz` 可到登录节点认证层。
- 更新 Mission Control 首屏 UI：新增“上线资源总控”面板，直接展示 DeepSeek、HPC/GPU、Claude Code、Kaggle 的真实配置状态和证据边界。
- 修正 DeepSeek 当前运行态：普通 dashboard 重启不会自动注入 DPAPI 密钥；已改用 `scripts\manage_deepseek_secret.ps1 restart-dashboard` 启动当前 8088 进程，live UI/API 恢复为 `DeepSeek Ready (deepseek-v4-flash)`。
- 收口资源状态显示语义：HPC/GPU 在 Web Terminal 证据通过后显示 `Verified / 已验证`，但不再泛化为 `Ready / 就绪`；自动 SSH 作业的远端公钥授权仍作为待办显示。
- 修正增强总览页 GPU 文案：当前缺口从 `nvidia-smi evidence` 更新为 `remote public-key authorization`，避免把已完成证据写成待办。
- 升级 AI 工作站前端视觉：新增 Mission Control 首屏 `AI Workstation Command Deck`，统一展示当前任务、主指标、4 x A800 GPU、任务/运行/动作统计与资源激活队列。
- 继续优化上线资源 UI：在“上线资源总控”下新增 `launch-evidence-strip`，为 DeepSeek、HPC/GPU、Claude Code、Kaggle 分别展示下一证据、状态和不含密钥的操作提示，避免把未完成资源误读为 ready。
- 接入启动审计可视化：Dashboard summary 读取 `docs\verified_workstation_launch_audit.json`，Mission Control 的“上线资源总控”新增 `Verified Launch Audit` 卡片，直接展示 DeepSeek/Claude/Kaggle DPAPI 加载状态、smoke 计数和审计报告路径。
- 升级全局设计系统：调整工作站背景、侧边栏、顶栏、状态条、Card/Button 阴影与交互层级，保持科研控制台风格，不改变业务闭环。
- Figma 设计方向板：已通过 Figma 插件创建 `Research Agent AI Workstation UI Upgrade`，文件地址 `https://www.figma.com/design/3SctfaIfPKAbovNBPnKxjq`。
- Figma 当前页面捕获：已通过 Figma 插件把 `http://127.0.0.1:8088` 当前工作站页面追加到同一设计文件，节点地址 `https://www.figma.com/design/3SctfaIfPKAbovNBPnKxjq?node-id=2-2`。捕获脚本仅临时注入，完成后已移除并重新构建生产包。
- DeepSeek 真实接入：新增 `scripts\manage_deepseek_secret.ps1`，使用 Windows DPAPI 存储 API Key；通过 `deepseek-v4-flash` 真实 smoke，返回 `deepseek-ok`。
- Kaggle 本地比分提质：House Prices seed sweep 后选定 seed=21，最新 run `20260613_232324`，`cv_rmsle_mean=0.12410`，优于上一版 `0.12899`。
- Claude Code 安全接入路径：新增 `scripts\manage_claude_secret.ps1`，使用 Windows DPAPI 存储 Anthropic Key；当前未安装 key 时 smoke 已验证 API/UI 明确返回 `not_configured` 并写入审计产物，不调用真实 Claude SDK。
- Kaggle 官方 token 安全接入路径：新增 `scripts\manage_kaggle_secret.ps1`，使用 Windows DPAPI 存储 `KAGGLE_USERNAME` / `KAGGLE_KEY`；当前未安装 token 时 smoke 明确返回 `not_configured`，本地 Kaggle-style 训练不受影响。
- 统一 verified 启动器：新增 `scripts\start_verified_workstation.ps1`，启动前自动加载已安装的 DeepSeek / Claude / Kaggle DPAPI 凭据，启动后执行 backend resource、DeepSeek、外部网关、Kaggle secret 和明文密钥扫描 smoke，避免普通重启漏注入密钥导致资源状态漂移。

## 关键证据

- Dashboard 地址：`http://127.0.0.1:8088`
- Dashboard 状态：`running`，HTTP 200，当前 API 汇总任务数 15。
- 全量验收：`python scripts\run_full_acceptance.py --dashboard-url http://127.0.0.1:8088`，结果 `passed`，`checks_run=38`。
- TypeScript：`npm run typecheck` 通过。
- Next build：`npm run build` 通过。
- Python 编译：`python -m compileall src scripts` 通过。
- Kaggle 新比赛 readiness：`python scripts\verify_kaggle_new_competition_readiness.py` 通过。
- 训练优化审计：`python scripts\verify_training_optimization_readiness.py --write-report` 通过，本地任务完成率 100%。
- House Prices 最新验证：`python scripts\validate_tabular_experiment.py --experiment-dir experiments\house_prices\20260613_232324 --config configs\house_prices.yaml` 通过。
- DeepSeek smoke：`powershell -ExecutionPolicy Bypass -File scripts\manage_deepseek_secret.ps1 smoke -Model deepseek-v4-flash -Port 8088` 通过。
- DeepSeek 启动链路：`powershell -ExecutionPolicy Bypass -File scripts\manage_deepseek_secret.ps1 restart-dashboard -Model deepseek-v4-flash -Port 8088` 已用于当前生产进程，资源激活显示 `2/4`。
- Verified 启动链路：`powershell -ExecutionPolicy Bypass -File scripts\start_verified_workstation.ps1 restart -Port 8088 -SkipFullAcceptance` 通过；`dpapi_loaded.deepseek=true`，`dpapi_loaded.claude=false`，`dpapi_loaded.kaggle=false`，DeepSeek smoke 和密钥扫描均通过。
- Verified 启动审计报告：`docs\verified_workstation_launch_audit.json`、`docs\verified_workstation_launch_audit.md` 已生成；`python scripts\verify_verified_workstation_launch_audit.py` 通过，确认审计报告未写入明文密钥。
- Verified 启动审计 UI：`python scripts\verify_scientific_ui_polish.py` 已保护 `verified-launch-audit-card`，浏览器全页截图确认该卡片在“上线资源总控”中实际渲染。
- Figma 捕获后生产包检查：`capture-script-absent`，确认 `html-to-design/capture.js` 未留在正式页面。
- Claude Code DPAPI 状态：`powershell -ExecutionPolicy Bypass -File scripts\manage_claude_secret.ps1 status` 返回 `not_configured`，未发现仓库明文 key。
- Claude Code 未配置 smoke：`powershell -ExecutionPolicy Bypass -File scripts\manage_claude_secret.ps1 smoke -Port 8088` 通过，状态 `not_configured_verified`，Manifest 写入 `workspace/code_agent_sessions/.../session_manifest.json`。
- Kaggle DPAPI 状态：`powershell -ExecutionPolicy Bypass -File scripts\manage_kaggle_secret.ps1 status` 返回 `not_configured`。
- Kaggle 未配置 smoke：`powershell -ExecutionPolicy Bypass -File scripts\manage_kaggle_secret.ps1 smoke` 返回 `not_configured`，缺少 `KAGGLE_USERNAME`、`KAGGLE_KEY`，不会进行官方 API 调用。
- 资源接入运行手册：`python scripts\verify_resource_activation_runbook.py` 保护 `start_verified_workstation.ps1`、DeepSeek/Claude/Kaggle DPAPI helper、HPC proxy bridge 和真实资源 smoke 流程。
- 最终交付一致性：`python scripts\verify_final_delivery_status.py --url http://127.0.0.1:8088 --require-json` 通过。
- 科研 UI 保护项：`python scripts\verify_scientific_ui_polish.py` 通过，包含 `launch_evidence_action_strip`。
- UI 布局质量：`python scripts\verify_ui_layout_quality.py --url http://127.0.0.1:8088` 通过。
- Dashboard 验证：`python scripts\verify_dashboard.py --url http://127.0.0.1:8088` 通过。
- HPC 代理桥：`powershell -ExecutionPolicy Bypass -File scripts\manage_hpc_proxy_bridge.ps1 test` 通过，返回 `SSH-2.0-SSHPiper`。
- HPC 代理桥状态：`scripts\manage_hpc_proxy_bridge.ps1 status` 显示 `running`，本机凭据已安装，监听 `127.0.0.1:7890`。
- HPC SOCKS 验证脚本：`python scripts\verify_hpc_socks_gateway.py --require-auth` 已支持 Windows DPAPI 本地桥，返回 `auth_mode=windows_dpapi_bridge` 和 `SSH-2.0-SSHPiper`。
- 明文凭据扫描：项目文件中未发现本次代理账号/HPC 密码明文。
- GPU Web 终端证据校验：`python scripts\verify_hpc_web_terminal_probe.py workspace\hpc\web_terminal_probe.txt` 通过，证明 `4 x A800`、CUDA、Python、文件系统和内存证据齐全。
- 上线 Go/No-Go 判定：新增 `scripts\verify_launch_go_no_go.py`，当前判定为 `go_local_ready_external_pending`；本地工作站可安全上线演示，外部资源不允许标记 fully ready。
- Go/No-Go 报告：`docs\launch_go_no_go_20260613.json`、`docs\上线Go-No-Go判定-20260613.md`。

## 视觉证据

- 桌面端最终截图：`docs/scientific_ui_desktop_final_20260612.png`
- 移动端菜单安全截图：`docs/scientific_ui_mobile_menu_safe_final_20260612.png`
- 可视化验收记录：`docs/可视化验收记录-20260612-科研质感补充.md`

说明：本轮内置浏览器已访问 `127.0.0.1:8088` 并确认首屏 `AI Workstation Command Deck`、资源激活 `2/4`、DeepSeek Ready、GPU Verified 和 `launch-evidence-strip` 实际渲染正常。页面由 `verify_dashboard.py`、live API、full acceptance 和浏览器截图共同验证为可访问。新增“上线资源总控”和下一证据行动条已纳入 `verify_scientific_ui_polish.py` 保护项。

## 新 Kaggle 比赛流程

- smoke 任务：`kaggle_new_competition_smoke`
- 配置：`configs/generated/kaggle_new_competition_smoke.yaml`
- 最新实验：`experiments/kaggle_new_competition_smoke/20260612_173516`
- 结果：本地 baseline、submission、validation gate 和 evidence 路径均可验证。
- 边界：官方 Kaggle 下载/提交需要 `KAGGLE_USERNAME`、`KAGGLE_KEY` 和人工 Gate。

## 外部资源接入边界

- DeepSeek：已通过 Windows DPAPI 用户级加密存储接入，live 状态为 `DeepSeek Ready (deepseek-v4-flash)`，真实 smoke 产物记录在 `workspace/llm/deepseek_smoke_*.json`。
- Claude Code：等待 Anthropic API Key。配置命令为 `powershell -ExecutionPolicy Bypass -File scripts\manage_claude_secret.ps1 install-key -ApiKey <key>`，随后用 `restart-dashboard` 注入运行进程；真实 SDK smoke 必须显式运行 `smoke -AllowRealExternal`。Claude 只能生成 transcript、patch 和 draft，应用代码仍需人工 Gate。
- GPU/HPC：`127.0.0.1:7890` 学校代理桥、PDF ncat 路径和 `100.85.169.63:1235` 登录节点可达；Web Terminal 证据已证明 4 x NVIDIA A800-SXM4-80GB、CUDA 12.8、Python、文件系统和内存状态。当前真实缺口是自动 SSH 作业：环境密码返回 partial success 后，服务端仍要求已授权公钥；需要平台或管理员把本机公钥绑定到环境 SSH 账号，或提供平台签发私钥。
- Kaggle 官方链路：等待 `KAGGLE_USERNAME`、`KAGGLE_KEY`。配置命令为 `powershell -ExecutionPolicy Bypass -File scripts\manage_kaggle_secret.ps1 install-token -Username <username> -Key <key>`；真实官方 API smoke 必须显式运行 `smoke -AllowRealExternal`，官方提交仍需 Human Gate。

## 给老师汇报时可说明

这个版本不是单纯静态 UI。它已经具备本地科研数据任务闭环、报告与图表生成、完整性 Gate、动作日志、Claude/GPU/Kaggle 接入骨架和未配置资源的安全阻断。后续拿到 API Key、GPU SSH 和 Kaggle token 后，不需要重做系统框架，按运行手册配置后即可进入真实资源 smoke test。
## 2026-06-14 收口复核

- Verified launcher 已重新启动 `http://127.0.0.1:8088`，并生成 `docs/verified_workstation_launch_audit.json` / `docs/verified_workstation_launch_audit.md`。
- `/api/workstation-summary` 已确认返回 `verified_launch_audit.status=passed`，`dpapi_loaded.deepseek=true`，`dpapi_loaded.claude=false`，`dpapi_loaded.kaggle=false`，`result_summaries=5`。
- 全量验收重新通过：`python scripts\run_full_acceptance.py --dashboard-url http://127.0.0.1:8088`，`checks_run=38`。
- UI 与资源状态重新留证：`docs/visual_acceptance_desktop.png`、`docs/visual_acceptance_mobile.png`、`docs/figma_current_page_capture.png`。
- Chrome/DOM 断言已确认 DeepSeek ready、GPU verified、Claude not configured、Kaggle not configured、启动审计卡片可见。
- 明文密钥扫描通过；精确口令标记扫描覆盖 45424 个文件，3 个敏感标记命中 0。
- Browser 插件可打开页面，但截图接口出现 `Root CDP client not initialized`；本轮已改用本机 Chrome headless 完成视觉验证。
