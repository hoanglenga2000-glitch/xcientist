# 系统完整上线修复与验收记录 - 2026-06-10

## 本轮修复范围

- 修复 Docker/Next 本地实验入口：容器内统一使用 `python3`，Python runner 内部使用 `sys.executable`，解决 House Prices 真实训练接口 500 问题。
- 升级 Code Quality Gate：导入 patch 后会检查最近 diff、危险命令、原始数据修改、凭证文件、文件范围，并执行 Python baseline 语法检查；Apply 前必须存在通过的 `code_quality_check_*.json`。
- 升级 Report Studio 图表生成：从空白 PNG 占位改为根据最新实验 artifacts 生成 6 张可读 SVG 图表，并支持插入报告、保存到 SQLite 与 workspace。
- 修复本机 validator 对 Docker `/app/...` 路径的兼容，确保容器生成的实验可以在 Windows 本机复验。
- 修复核心 UI 响应式布局：Mission Control、Code Runner、Integrity Gates、Agent Runtime、Experiments、Design/Overview 的固定列改为窄屏堆叠、宽屏多栏。

## 真实业务流程验证

- 通过前端 API 触发 House Prices 本地训练：
  - 最新实验目录：`experiments/house_prices/20260610_070259`
  - best model：`gradient_boosting_log_target`
  - CV RMSLE：`0.128761`
  - Holdout RMSLE：`0.129797`
  - Submission 行数：`1459`
  - Validation gate：`passed`

## 接口闭环验证

- `GET /api/workstation-summary`：通过，返回三任务运行状态。
- `GET/PATCH /api/settings`：通过，Settings 持久化到 SQLite。
- `POST /api/tasks/house_prices/export-code-agent-context`：通过，导出 `workspace/tasks/house_prices/code_agent_context`。
- `POST /api/tasks/house_prices/import-agent-patch`：通过，导入 patch diff。
- `POST /api/workstation-actions review_agent_patch`：通过，生成 patch review、quality gate、diff、trace artifacts。
- `POST /api/workstation-actions apply_agent_patch`：通过，必须在 quality gate passed 后记录 apply。
- `POST /api/tasks/house_prices/generate-figures`：通过，生成 6 张 SVG 图表。
- `PATCH /api/tasks/house_prices/report`：通过，保存 Markdown/HTML 草稿。
- `POST /api/reports/house_prices_latest_report/insert-figure`：通过，图表写入报告草稿。

## 自动化验收结果

- `npm run typecheck`：passed
- `npm run build`：passed
- `python -m compileall src scripts`：passed
- `python scripts/validate_titanic_experiment.py --experiment-dir experiments/titanic/20260609_231238 --config configs/titanic.yaml`：passed
- `python scripts/validate_tabular_experiment.py --experiment-dir experiments/house_prices/20260610_070259 --config configs/house_prices.yaml`：passed
- `python scripts/verify_research_sources.py`：passed
- `python scripts/verify_research_integrity.py`：passed
- `python scripts/verify_dashboard.py --url http://127.0.0.1:8088`：passed
- `python scripts/verify_runtime_completeness.py`：passed
- `python scripts/verify_workstation_action_contract.py`：passed
- `python scripts/verify_code_agent_patch_lifecycle.py`：passed
- `python scripts/run_full_acceptance.py --dashboard-url http://127.0.0.1:8088`：passed，12 checks。

## 浏览器验收

- 已打开 `http://127.0.0.1:8088` 验证 Mission Control、Code Runner、Report Studio、Settings、Integrity Gates。
- 已实点 Code Runner 的 context export / patch review，Report Studio 的 generate figures / save draft，Settings 的 save settings。
- 1440、920、536 宽度复测：920 宽关键页面无主内容溢出；536 宽页面不撑破浏览器，代码/画布类区域保留内部滚动。

## 仍保持受控预留

- Kaggle 官方下载/leaderboard submit 未启用：本机未配置 Kaggle token，系统保持 `Reserved / Not Configured`，不伪造提交。
- GPU/远程 runner 未启用：当前使用本地 CPU/Python runner，GPU 作为后续可替换接口。
- LLM 外部 API 未启用：当前以 rule-based/local template 为主，Claude Code/Codex 通过上下文导出、patch 导入和质量门接入。
