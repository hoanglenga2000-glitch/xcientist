# 科研 Agent 工作站上线冒烟测试最终状态

- 生成时间：2026-06-11 19:20:19
- Dashboard：http://127.0.0.1:8088
- 当前状态：ready_for_external_resources
- 结论：本地系统、前端构建、完整验收、报告图表、Claude Code SDK 网关、GPU SSH 安全阻断均已通过上线冒烟测试；正式完全外部上线只等待 GPU SSH 凭证。

## 已通过

- 仪表盘服务运行正常，HTTP 200。
- `npm run typecheck` 通过。
- `npm run build` 通过。
- 生产 smoke 已支持单项超时、构建后自动重启，避免长时间卡住。
- 完整验收 `run_full_acceptance.py` 通过 30 项检查。
- House Prices、Titanic、Telco Churn 三个本地任务验证通过。
- 报告编辑页支持图表生成、图表插入、目录跳转和宽预览。
- Claude Code 已通过真实 Claude Agent SDK smoke，生成 transcript 和 reviewable patch。
- GPU SSH 未配置时返回 `not_configured`，写入审计产物，并拒绝非白名单命令。
- 强制真实资源模式会因 GPU 缺失失败，不会伪造全绿。
- 生产冒烟报告在 PowerShell 和 WSL 中均可正常显示中文。

## 受控未完成

- GPU SSH 尚未配置，缺少：
  - `GPU_SSH_HOST`
  - `GPU_SSH_USER`
  - `GPU_SSH_KEY_PATH`
  - `GPU_REMOTE_WORKSPACE`
- Kaggle 官方 API 仍为可选扩展，缺少：
  - `KAGGLE_USERNAME`
  - `KAGGLE_KEY`

## 关键证据

- `docs/production_smoke_audit.json`
- `docs/正式上线全面冒烟测试报告-20260611.md`
- `workspace/code_agent_sessions/claude_2026-06-11T11-20-01-862Z_mukioh/session_manifest.json`
- `workspace/tasks/house_prices/code/patches/claude_agent_claude_2026-06-11T11-20-01-862Z_mukioh.diff`

## 最终复测命令

```powershell
python scripts\run_production_smoke_audit.py --dashboard-url http://127.0.0.1:8088 --container-name research-agent-workstation --write-report
```

配置 GPU SSH 后运行：

```powershell
python scripts\run_real_resource_smoke.py --dashboard-url http://127.0.0.1:8088 --container-name research-agent-workstation --require-configured
```
