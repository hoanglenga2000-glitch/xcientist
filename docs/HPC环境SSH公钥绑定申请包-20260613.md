# HPC 环境 SSH 公钥绑定申请包 - 2026-06-13

## 目的

将科研 Agent 工作站的 GPU 自动作业通道从“GPU 容器已验证 / Job Key Pending”推进到“白名单 GPU Job 可自动提交”。

当前已证明：

- 校外代理桥 `127.0.0.1:7890` 可到达登录节点 `100.85.169.63:1235`，返回 `SSH-2.0-SSHPiper`。
- Web Terminal 证据已证明 GPU 容器内可见 `4 x NVIDIA A800-SXM4-80GB`。
- 环境 SSH 登录表现为：密码认证 `partial success` 后继续要求已授权 public key。
- 门户账号可以进入平台 Web 与模型开发 UI，但在已检查的 `jhai/aiBase` 开发环境前端模块中未找到自助 SSH 公钥绑定接口。

## 需要平台侧绑定的 public key

请将以下公钥绑定到 GPU 环境 SSH 账号 `aimslab-zvrLqshT`：

```text
ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIN6/d3MqazV4Q3UA3keZwSpUFIDXwflD1mHYpArfCtYx research-agent-hpc-20260613
```

本机对应私钥路径：

```text
C:\Users\景浩伟\.ssh\research_agent_hpc_ed25519
```

不要绑定门户/HPC 平台账号密码；不要把密码写入 `authorized_keys`、配置文件、SQLite 或项目仓库。

## 绑定后验证命令

保持本地代理桥运行：

```powershell
powershell -ExecutionPolicy Bypass -File scripts\manage_hpc_proxy_bridge.ps1 test
```

设置 GPU 作业网关环境变量：

```powershell
$env:GPU_SSH_HOST="100.85.169.63"
$env:GPU_SSH_PORT="1235"
$env:GPU_SSH_USER="aimslab-zvrLqshT"
$env:GPU_SSH_KEY_PATH="C:\Users\景浩伟\.ssh\research_agent_hpc_ed25519"
$env:GPU_REMOTE_WORKSPACE="/hpc2ssd/JH_DATA/spooler/aimslab"
$env:GPU_SSH_SOCKS_HOST="127.0.0.1"
$env:GPU_SSH_SOCKS_PORT="7890"
```

重启 dashboard 并验证：

```powershell
python scripts\manage_workstation_dashboard.py restart --port 8088 --force --timeout 90
python scripts\verify_external_resource_gateways.py --url http://127.0.0.1:8088 --allow-real-external
python scripts\run_full_acceptance.py --dashboard-url http://127.0.0.1:8088
```

验收标准：

- `/api/gpu/connections/test` 返回 `status=passed`。
- GPU Job 只允许白名单模板，不开放任意 shell。
- artifact 中记录 host、workspace、proxy policy、stdout/stderr，但不记录密码。
- `python scripts\verify_no_plaintext_secrets.py` 通过。

## 证据文件

- `workspace/hpc/web_terminal_probe.txt`
- `workspace/hpc/ssh_auth_mode_20260613.json`
- `workspace/hpc/portal_key_binding_probe_20260613.json`
